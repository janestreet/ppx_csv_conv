(library
 ((name ppx_csv_conv_test)
  (libraries (core csvfields))
  (preprocess (pps (ppx_jane ppx_csv_conv)))
  (inline_tests ((deps (test.csv)))))
)

(alias ((name DEFAULT) (deps (example.ml.pp example.mli.pp))))

(jbuild_version jane_street)
